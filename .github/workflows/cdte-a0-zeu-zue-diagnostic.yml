name: cdte-a0-zeu-zue-diagnostic

# The one authorized real execution completed in run 29944282253.
# Pull-request execution is disabled to enforce the no-retry decision.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  bounded-diagnostic:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Validate repository parser and tests only
        run: |
          python -m pip install -e ".[test]"
          python -m pytest -vv tests/test_cdte_a0_zeu_zue.py

      - name: Refuse automatic scientific rerun
        run: |
          echo "The authorized real diagnostic already completed in run 29944282253."
          echo "No retry, convergence sweep, altered calculation, or A1 work is authorized."
          exit 1
