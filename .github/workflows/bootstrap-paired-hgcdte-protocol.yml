name: bootstrap-paired-hgcdte-protocol

on:
  pull_request:
    paths:
      - '.github/paired_protocol_bundle.part*'
      - '.github/workflows/bootstrap-paired-hgcdte-protocol.yml'

permissions:
  contents: read

jobs:
  extract:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7

      - name: Verify package identity
        shell: bash
        run: |
          cat .github/paired_protocol_bundle.part* > /tmp/paired_protocol.b64
          test "$(wc -c < /tmp/paired_protocol.b64)" -eq 25392
          echo "a6ddc3fc75d510bfc2607bd04a9adbf890d4d1cc0978897583c314c09b661eeb  /tmp/paired_protocol.b64" | sha256sum -c -
          base64 --decode /tmp/paired_protocol.b64 > /tmp/paired_protocol.tar.gz
          test "$(wc -c < /tmp/paired_protocol.tar.gz)" -eq 19043
          echo "4db730c660c8d91c4e9c750317e3f1cb0360998478b67bead41fb452a3dd03e7  /tmp/paired_protocol.tar.gz" | sha256sum -c -

      - name: Extract package safely
        run: |
          python - <<'PY'
          from pathlib import Path
          import tarfile

          root = Path('extracted').resolve()
          root.mkdir()
          with tarfile.open('/tmp/paired_protocol.tar.gz', 'r:gz') as stream:
              for member in stream.getmembers():
                  target = (root / member.name).resolve()
                  if root not in target.parents and target != root:
                      raise SystemExit(f'unsafe archive member: {member.name}')
              stream.extractall(root)
          PY

      - name: Upload extracted package
        uses: actions/upload-artifact@v4
        with:
          name: paired-hgcdte-protocol-package
          path: |
            extracted/protocols/hgcdte_paired_gap_acquisition_protocol.md
            extracted/data/templates/hgcdte_paired_gap_acquisition_template.json
            extracted/tools/audit_hgcdte_paired_gap_acquisition.py
            extracted/data/derived/hgcdte_paired_gap_protocol_reference_audit.json
            extracted/tests/test_hgcdte_paired_gap_protocol.py
            extracted/research/decision_records/2026-07-19-paired-hgcdte-acquisition-protocol.md
          if-no-files-found: error
          retention-days: 30
