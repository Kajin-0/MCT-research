# SST submission bundle

This directory contains repository-visible generated manuscript sources and review PDFs.

The files are generated from the controlled Markdown manuscript by `tools/build_sst_submission_bundle_compat.py`. The generated files are committed so the manuscript can be reviewed directly from the repository tree; the builder and validation tests remain the source of truth.
